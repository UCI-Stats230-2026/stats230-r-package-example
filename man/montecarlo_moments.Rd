% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/homework5.R
\name{montecarlo_moments}
\alias{montecarlo_moments}
\title{Monte Carlo Estimation of Moments}
\usage{
montecarlo_moments(
  rv_distribution = list(sampler = function(n) rgamma(n, shape = 4, rate = 2), density =
    function(x) dgamma(x, shape = 4, rate = 2)),
  moment = 1,
  type = c("importance", "naive"),
  n_samples = 1000,
  ci_level = 0.95,
  instrument_distribution = list(sampler = function(n) rlnorm(n, meanlog = log(2) -
    log(1.25)^2/2, sdlog = log(1.25)), density = function(x) dlnorm(x, meanlog = log(2) -
    log(1.25)^2/2, sdlog = log(1.25)))
)
}
\arguments{
\item{rv_distribution}{A list describing the target distribution.
For \code{type = "naive"}, it must include \code{sampler}, a function with argument
\code{n} that returns random draws from the target.
For \code{type = "importance"}, it must include \code{density}, a function with
argument \code{x} that evaluates the target density at \code{x}.}

\item{moment}{Numeric scalar giving the moment order $k$ in
$\eqn{E(X^k)}$. Default is \code{1}.}

\item{type}{Character string indicating estimator type:
\code{"importance"} (default) or \code{"naive"}.}

\item{n_samples}{Positive numeric scalar. Number of Monte Carlo samples.
Default is \code{1000}.}

\item{ci_level}{Numeric scalar in \verb{(0, 1)}. Confidence level used for the
normal-approximation confidence interval. Default is \code{0.95}.}

\item{instrument_distribution}{A list describing the proposal distribution
used when \code{type = "importance"}. Must include \code{sampler}, a function with
argument \code{n} that returns proposal draws, and \code{density}, a function with
argument \code{x} that evaluates the proposal density.}
}
\value{
A list with:
\describe{
\item{estimate}{Numeric scalar estimate of the requested moment.}
\item{mc_error}{Estimated Monte Carlo standard error.}
\item{conf_int}{Length-2 numeric vector with lower and upper confidence
bounds.}
}
}
\description{
Estimates $\eqn{E(X^k)}$ for a random variable $X$ using either
naive Monte Carlo sampling from the target distribution or
importance sampling.
}
